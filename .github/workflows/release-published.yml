name: Release Extension to Sonatype

on:
  workflow_dispatch:
  release:
    types: [published]

jobs:
  release:
    uses: liquibase/build-logic/.github/workflows/extension-release-published.yml@afaacfdeb9cc754b4c3fc91a718be7283491b879 # v0.7.8
    secrets: inherit
