name: Attach Artifact to Release

on:
  pull_request:
    types:
      - closed

permissions:
  contents: write
  actions: read
  packages: write
  id-token: write

jobs:
  attach-artifact-to-release:
    uses: liquibase/build-logic/.github/workflows/extension-attach-artifact-release.yml@afaacfdeb9cc754b4c3fc91a718be7283491b879 # v0.7.8
    secrets: inherit